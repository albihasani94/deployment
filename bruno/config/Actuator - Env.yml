info:
  name: Actuator > Env
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8071/actuator/env
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: false
  maxRedirects: 5
