info:
  name: openid-configuration
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8082/realms/spring-microservices/.well-known/openid-configuration
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
