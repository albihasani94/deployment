info:
  name: Organization
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8072/organization-service/v1/organization/1
  headers:
    - name: X-Correlation-Id
      value: demo-123
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
